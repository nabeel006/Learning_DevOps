
```shell
   ssh-keygen -t rsa -b 4096 -C "nabeel@gmail.com"
   #Testing the connection
   ssh -T git@github.com

```

```bash

	git add .
    git status
    git config --global user.email "Nabeel@gmail.com"
    git config --global user.name "Nabeel"
    git config --global --list
    git commit -m "Learning AWS ECS Service"
	git branch
	git push -u origin main


```
